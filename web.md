# 🌐 Web & CSS Interview Questions (2025)



## 🔹 20 Web (HTML, Browser, Network) Interview Questions

1. What happens when you type a URL in the browser and press Enter?
2. What is the difference between HTTP and HTTPS?
3. What is CORS and why does it exist?
4. What are cookies, localStorage, and sessionStorage? How are they different?
5. What is the difference between `POST` and `PUT` requests?
6. What are semantic HTML elements and why are they important?
7. What is the difference between `div` and `span`?
8. What is the purpose of the `<meta>` tag?
9. What is the difference between `async` and `defer` attributes in script tags?
10. What is DNS and how does it work?
11. What is the difference between client-side rendering and server-side rendering?
12. What is the Same-Origin Policy?
13. What are web accessibility (a11y) basics?
14. What is ARIA and when should it be used?
15. What is the difference between `<script>` placed in `<head>` vs `<body>`?
16. What is a CDN and why is it used?
17. What are HTTP status codes? Explain common ones.
18. What is caching and how does browser caching work?
19. What is progressive web app (PWA)?
20. What is SEO and how does HTML structure affect it?

---

## 🎨 20 CSS Interview Questions

1. What is the CSS box model?
2. What is the difference between `display: none` and `visibility: hidden`?
3. What is CSS specificity and how is it calculated?
4. What is the difference between `inline`, `inline-block`, and `block`?
5. What is the difference between `position: relative`, `absolute`, `fixed`, and `sticky`?
6. What is Flexbox and what problems does it solve?
7. What is the difference between Flexbox and CSS Grid?
8. What is `z-index` and when does it work?
9. What are pseudo-classes and pseudo-elements?
10. What is responsive design?
11. What are media queries and how do they work?
12. What is the difference between `em`, `rem`, `%`, `vh`, and `vw` units?
13. What is `overflow` and its different values?
14. What is `opacity` vs `visibility`?
15. What is `object-fit` and when is it used?
16. What is `display: flex` vs `display: grid`?
17. What is the difference between `margin` and `padding`?
18. What is `!important` and why should it be avoided?
19. What are CSS preprocessors? Have you used Sass or Less?
20. What are CSS animations and transitions? What is the difference?

---
