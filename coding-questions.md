# 🚀 JavaScript Coding Questions

Below are **20 frequently asked JavaScript coding questions**
👉 *Only questions are listed. No answers included.*

---

## 🟡 JavaScript Coding Questions

1. Write a program to **remove duplicates from an array**.

2. Find the **maximum count of consecutive 1’s** in a binary array.

3. Write a JavaScript program to **find the largest element in a nested array**.

4. Implement a function to **flatten a deeply nested array** into a single-dimensional array.

5. Write a function to **find the second largest element** in an array.

6. Convert a **string input into an object**.
   - Example: `"a:1,b:2,c:3"`

7. Write a function to **check if two strings are anagrams**.

8. Find the **first non-repeating character** in a string.

9. Write a program to **reverse words in a sentence**.
   - Example: `"Hello World"` → `"World Hello"`

10. Implement a function to **count the frequency of elements** in an array.

11. Write a function to **deep clone an object**.

12. Find the **intersection of two arrays**.

13. Write a program to **merge two sorted arrays** into one sorted array.

14. Implement a function to **check if a string is a palindrome**.

15. Write a function to **group objects by a property**.
   - Example: group users by `age` or `role`

16. Find the **longest word in a string**.

17. Write a function to **sum all numbers in a nested object**.

18. Implement **debounce** or **throttle** from scratch.

19. Write a program to **rotate an array by `k` steps**.

20. Write a function to **find missing numbers in a given range**.

---
